require 'bundler'
Bundler::GemHelper.install_tasks

namespace :ci do
  desc "setup for continuous integration"
  task :prepare do
    # nothing to do here
    # the task is necessary for our CI environment
  end
end

